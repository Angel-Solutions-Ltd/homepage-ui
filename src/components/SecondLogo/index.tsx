import './index.css';

interface P {

}

export default ({}: P) => {
    return (
        <div className={`content-page second-logo-page`}>
            <h1 className={`letter-holder`}>AS</h1>
        </div>
    );
}